# DecisionPassport

**Цифровой контур принятия управленческих решений для предприятия атомной отрасли.**

Платформа реализует научный результат магистерской диссертации «Совершенствование процессов принятия управленческих решений в системе корпоративного управления предприятием атомной отрасли в условиях цифровой трансформации» (НИЯУ МИФИ, 2026). Эмпирическая база — АО «НАК «Казатомпром».

Единица управления в системе — не документ и не отчёт, а **управленческий вопрос**, получающий полный цифровой след: инициатор, цель, данные с указанием источника, альтернативы, риски, расчёты, согласования, решение, поручения, фактический результат и извлечённые уроки. Этот след материализуется в объекте «Цифровой паспорт управленческого решения».

---

## Три принципа, зашитые в код и UX

| Принцип | Как обеспечен технически |
| --- | --- |
| **Human-in-the-loop.** Система не принимает решение за человека | Переход со стадии «Решение» невозможен, пока `status ≠ APPROVED` ([lib/gate-service.ts](lib/gate-service.ts)); утверждение требует явного выбора альтернативы и мотивировки уполномоченным человеком (`BOARD_MEMBER`, а в демо-контуре также аудируемый `ADMIN`); ни одно предложение ИИ не применяется без вердикта человека (`AiSuggestion.humanVerdict`), «Отклонить рекомендацию» — равноправная кнопка |
| **Сначала данные и процесс — затем интеллект** | Аналитическая ступень ИИ открывается только для показателей с заполненными источником и владельцем; рекомендательная — только при наличии валидированной модели в реестре, допущенной для данного уровня критичности ([lib/ai/eligibility.ts](lib/ai/eligibility.ts)) |
| **Прослеживаемость.** Любое число кликабельно до источника | Компонент [components/provenance.tsx](components/provenance.tsx) у каждого значения раскрывает систему-источник, дату актуальности, владельца и формулу; карточка показателя содержит граф происхождения данных |

---

## Запуск

```bash
npm install
npm run db:push     # создание схемы БД (SQLite по умолчанию)
npm run seed        # демо-данные: 9 пользователей, 12 решений, 20 показателей
npm run dev         # http://localhost:3000
```

Терминал с `npm run dev` должен оставаться открытым. Если браузер показывает
`ERR_CONNECTION_REFUSED`, сервер не запущен: повторите `npm run dev`, дождитесь строки
`Ready` и откройте `http://localhost:3000/login`.

Приложение **полностью работает без единого внешнего API-ключа**: при отсутствии `AI_API_KEY` используется детерминированный `MockProvider`, строящий ответы из данных паспорта.

Прочие команды:

```bash
npm run typecheck      # TypeScript strict, без any и @ts-ignore
npm run lint           # ESLint
npm run test           # Vitest: бизнес-логика + onboarding registry/progress
npm run test:e2e       # Playwright: lifecycle, responsive и guided onboarding
npm run build          # production-сборка
```

Для запуска из PowerShell на текущем компьютере:

```powershell
cd "C:\Users\almas\OneDrive\Desktop\ms\decision-passport"
npm.cmd run dev
```

После строки `Ready` откройте <http://localhost:3000/login>. Закрытие терминала останавливает локальный сервер.

### PostgreSQL вместо SQLite

Схема написана переносимо (перечисления хранятся как `String`, а не как Prisma-enum). Для перехода:

1. в [prisma/schema.prisma](prisma/schema.prisma) заменить `provider = "sqlite"` на `provider = "postgresql"`;
2. в `.env` указать `DATABASE_URL="postgresql://user:pass@localhost:5432/decision_passport"`;
3. выполнить `npm run db:push && npm run seed`.

Код приложения менять не требуется.

---

## Демо-аккаунты

Пароль у всех — `demo1234`. На экране входа доступен вход одним кликом по карточке роли.

| Email | Пользователь | Роль | Что может в системе |
| --- | --- | --- | --- |
| `initiator@kap.kz` | Динара Ахметова | INITIATOR | Создаёт паспорта, заполняет блоки, отправляет на экспертизу, ведёт поручения и уроки |
| `dataowner@kap.kz` | Ержан Смагулов | DATA_OWNER | Подтверждает качество показателей производственного блока |
| `dataowner2@kap.kz` | Гульнара Оспанова | DATA_OWNER | Подтверждает качество показателей финансового блока |
| `risk@kap.kz` | Тимур Бекетов | RISK_OFFICER | Заполняет риск-профиль, участвует в независимой проверке расчётов |
| `analyst@kap.kz` | Алия Нурланова | ANALYST | Каталог показателей, реестр моделей, расчёты эффекта, запуск ступеней ИИ |
| `analyst2@kap.kz` | Марат Касымов | ANALYST | Независимая проверка критических расчётов (второй пользователь) |
| `secretary@kap.kz` | Сауле Жумабаева | SECRETARY | Проверяет маршрут и компетенцию органа, возвращает на доработку |
| `board@kap.kz` | Нурлан Абишев | BOARD_MEMBER | Ролевые дашборды, принятие решения с мотивировкой, вердикт по ИИ |
| `admin@kap.kz` | Администратор системы | ADMIN | Пользователи, справочники и правила; в демо-контуре также расширенные аудируемые полномочия восстановления |

Права проверяются **на сервере** в каждом server action и route handler через [lib/authz.ts](lib/authz.ts), а не только скрытием элементов интерфейса.

---

## Guided onboarding

Платформа имеет три независимых режима обучения:

- **PAGE** автоматически объясняет ещё не изученную страницу или вкладку при первом посещении текущей версии;
- **ROLE** проводит через сквозной рабочий маршрут конкретной роли;
- **THESIS / JURY** за 10–15 минут раскрывает научную модель от управленческого вопроса до post-evaluation, уроков и baseline → pilot.

Кнопка «Обучение» доступна на `/login` и в верхней панели приложения. Через неё можно повторить текущую инструкцию, запустить ролевой или jury-сценарий и сбросить только учебный прогресс. Решения, пользователи, показатели и аудит при этом не изменяются.

Прогресс хранится локально по ключу `userId + role + tourId + version`. Семантика состояний различается: «Готово» — `completed`, «Пропустить» — `skipped`, крестик или `Escape` — `dismissed`; ни одно из этих действий не завершает остальные tours. Центральный registry находится в [lib/onboarding](lib/onboarding), стабильные UI-контракты используют `data-tour`, а карта покрытия — в [ONBOARDING-COVERAGE.md](ONBOARDING-COVERAGE.md).

Самостоятельный сценарий защиты описан в [JURY-SELF-GUIDED-SCENARIO.md](JURY-SELF-GUIDED-SCENARIO.md), визуальный протокол — в [ONBOARDING-VISUAL-QA.md](ONBOARDING-VISUAL-QA.md), эталонные кадры создаются в `.artifacts/onboarding-final/`.

---

## Реализованные экраны

| Экран | Маршрут |
| --- | --- |
| Вход с ролями, сгруппированными по контуру ответственности | `/login` |
| Дашборд руководителя | `/dashboard` |
| Глобальный поиск решений, показателей, моделей и поручений | `/search?q=...` |
| Реестр решений с фильтрами и поиском | `/decisions` |
| Создание паспорта | `/decisions/new` |
| **Паспорт решения** (9 блоков, степпер 7 стадий, чек-лист ворот) | `/decisions/[id]?tab=passport` |
| Сравнение альтернатив (матрица, радар, выбор с мотивировкой) | `/decisions/[id]?tab=alternatives` |
| Риск-профиль и ключевые допущения | `/decisions/[id]?tab=risks` |
| Экономика: калькуляторы (1)–(3) и независимая проверка | `/decisions/[id]?tab=economics` |
| Поручения с обязательной связью с KPI | `/decisions/[id]?tab=assignments` |
| AI Analysis Workspace: три ступени аналитики и обязательный вердикт человека | `/decisions/[id]?tab=ai` |
| Аудит паспорта | `/decisions/[id]?tab=audit` |
| Каталог показателей | `/indicators` |
| Карточка показателя: история значений и data lineage | `/indicators/[id]` |
| Замер эффекта и KPI «до/после», индекс зрелости | `/kpi` |
| Реестр моделей ИИ | `/models` |
| Журнал решений и база уроков | `/lessons` |
| Ролевые дашборды: стратегическая, инвестиционная, производственная, риск-панель | `/boards` |
| Дорожная карта внедрения с KPI-гейтами | `/roadmap` |
| Сквозной журнал аудита | `/audit` |
| Администрирование: пользователи, справочники, правила ворот | `/admin` |

---

## Архитектура

```
app/
  (app)/                    защищённая зона: compact rail + contextual workspace
    dashboard/ decisions/ indicators/ kpi/ models/
    lessons/ boards/ roadmap/ audit/ admin/
  actions/                  server actions (мутации): проверка прав → изменение → AuditEvent
    decisions.ts evidence.ts ai.ts indicators.ts admin.ts
  api/
    auth/[...nextauth]/     NextAuth (Credentials, JWT)
    decisions/[id]/advance/ единственная точка перехода по стадиям
lib/
  onboarding/                registry PAGE / ROLE / THESIS, resolver и local progress
  domain.ts                 перечисления и zod-схемы предметной области
  gates.ts                  чистые функции правил контрольных ворот
  gate-service.ts           серверный слой: конфигурация из БД + переход по стадиям
  snapshot.ts               снимок решения и расчёт полноты 9 блоков
  effects.ts                формулы (1) Eₜ, (2) Eᵣ, (3) NPV
  kpi.ts / maturity.ts      пять групп KPI и индекс зрелости процесса
  analytics.ts              агрегаты для дашбордов
  authz.ts / audit.ts       матрица прав и сквозной аудит
  ai/provider.ts            MockProvider (дефолт) и ApiProvider (откат на mock)
  ai/eligibility.ts         допуск к ступеням ИИ
  connectors/               заглушки SAP / eKAP / Power BI / DWH с единым fetchIndicator(code)
  i18n/ru.ts                все интерфейсные строки (готово к добавлению KZ/EN)
components/
  onboarding/               spotlight, coach card, Help и graceful error boundary
  decision/                 паспорт: шапка, степпер, чек-лист ворот, панели вкладок
  charts/                   Decision Flow, парное сравнение KPI, история значений
  provenance.tsx            происхождение любого числа
  lineage-graph.tsx         семантический путь source → evidence → decision → authority
prisma/                     схема данных и сиды
tests/ e2e/                 Vitest и Playwright
```

### Ключевые архитектурные решения

1. **Ворота — чистые функции.** Правила в [lib/gates.ts](lib/gates.ts) работают над снимком решения (`GateSnapshot`) и не знают о БД, поэтому покрыты юнит-тестами целиком. Таблица `GateCheck` хранит только *применимость* правила (переход × уровень критичности × блокирующее ли), а сами проверки — код. Администратор меняет конфигурацию через `/admin`, не трогая логику.
2. **Единственная точка перехода** — `POST /api/decisions/:id/advance`. UI не решает, можно ли двигаться дальше: он показывает чек-лист, а допуск определяет сервер.
3. **Мутации через server actions** с единым контрактом `ActionResult` (`{ok:true}` / `{ok:false, error}`), проверкой прав и записью `AuditEvent` в той же транзакции.
4. **Полнота блоков вычисляется, а не хранится «на веру»** — из фактического содержимого паспорта ([lib/snapshot.ts](lib/snapshot.ts)); поле `completeness` лишь кэширует расчёт.
5. **Деньги считаются только из введённых параметров.** Функции эффекта возвращают либо результат, либо `{ok:false, missing:[...]}`; интерфейс выводит «Недостаточно данных для расчёта» с перечнем незаполненных параметров, но никогда — оценку по умолчанию.
6. **Разделение природы числа.** Три визуально различных бейджа: факт, прогноз, допущение. Прямой и косвенный экономический эффект разделены визуально; к риск-эффекту применяется консервативный коэффициент атрибуции (по умолчанию 0.5) с обязательным полем обоснования.
7. **Индекс зрелости всегда с контекстом.** В коде нет пути, где он выводится без размера выборки, периода, перечня учтённых KPI и пометки «расчёт по данным пилота — не официальный показатель компании».

---

## Соответствие разделов диссертации модулям системы

| Раздел диссертации | Модуль системы | Файлы |
| --- | --- | --- |
| Управленческое решение как объект цифрового управления; цифровой след решения | Паспорт решения: 9 блоков, полный жизненный цикл | [app/(app)/decisions/[id]/page.tsx](app/\(app\)/decisions/[id]/page.tsx), [lib/snapshot.ts](lib/snapshot.ts) |
| Семь стадий цикла принятия решения | Степпер стадий и переход только через API | [components/stage-stepper.tsx](components/stage-stepper.tsx), [app/api/decisions/[id]/advance/route.ts](app/api/decisions/[id]/advance/route.ts) |
| Контрольные ворота и дифференциация требований по уровням критичности A/B/C | Движок ворот и конфигурируемые правила | [lib/gates.ts](lib/gates.ts), [lib/gate-service.ts](lib/gate-service.ts), `/admin` |
| Качество данных: владелец, источник, актуальность, прослеживаемость | Каталог показателей, data lineage, подтверждение качества | [app/(app)/indicators/](app/\(app\)/indicators/), [components/lineage-graph.tsx](components/lineage-graph.tsx) |
| Интеграционный ландшафт (ERP, BI, RPA, хранилище) | Слой коннекторов с единым интерфейсом | [lib/connectors/](lib/connectors/) |
| Сравнение альтернатив по единому набору критериев; обязательный вариант «статус-кво» | Матрица «альтернативы × критерии», лепестковая диаграмма | [components/decision/alternatives-panel.tsx](components/decision/alternatives-panel.tsx) |
| Управление рисками решения: остаточный риск, владелец, триггеры пересмотра | Риск-профиль, матрицы до/после мер | [components/decision/risks-panel.tsx](components/decision/risks-panel.tsx) |
| Ключевые допущения как условие обоснованности решения уровня A | Реестр допущений с датой действия | [components/decision/assumptions-panel.tsx](components/decision/assumptions-panel.tsx) |
| Методика оценки экономического эффекта: формулы (1)–(3) | Калькуляторы и независимая проверка расчётов | [lib/effects.ts](lib/effects.ts), [components/effect-calculators.tsx](components/effect-calculators.tsx) |
| Пять групп KPI процесса; режим «до/после» | Замер эффекта на сопоставимых выборках | [lib/kpi.ts](lib/kpi.ts), [app/(app)/kpi/page.tsx](app/\(app\)/kpi/page.tsx) |
| Индекс зрелости процесса принятия решений (5 уровней) | Расчёт с обязательным контекстом | [lib/maturity.ts](lib/maturity.ts), [components/maturity-card.tsx](components/maturity-card.tsx) |
| Трёхступенчатая модель применения ИИ и границы ответственности | ИИ-помощник, реестр моделей, вердикт человека | [lib/ai/](lib/ai/), [components/decision/ai-panel.tsx](components/decision/ai-panel.tsx), `/models` |
| Организация исполнения: поручения, связанные с результатом | Поручения с обязательной связью с KPI | [components/decision/assignments-panel.tsx](components/decision/assignments-panel.tsx) |
| Пост-оценка и организационное обучение | База уроков, категории причин отклонений | [components/decision/post-evaluation-panel.tsx](components/decision/post-evaluation-panel.tsx), `/lessons` |
| Дорожная карта внедрения по этапам с условиями перехода | 4 этапа с KPI-гейтами и матрица рисков внедрения | [lib/roadmap.ts](lib/roadmap.ts), `/roadmap` |
| Корпоративное управление: разграничение компетенций органов | Ролевые дашборды комитетов, единая доказательная база | `/boards` |
| Контрольная среда и подотчётность | Сквозной аудит всех мутаций | [lib/audit.ts](lib/audit.ts), `/audit` |

---

## Тестовое покрытие

**Vitest — 97 юнит-тестов:**

- `tests/effects.test.ts` — три формулы эффекта, включая поведение при незаполненных параметрах;
- `tests/gates.test.ts` — все 11 правил контрольных ворот и их применимость по уровням A/B/C;
- `tests/maturity.test.ts` — нормирование KPI и расчёт индекса зрелости;
- `tests/authz.test.ts` — матрица прав по всем ролям.
- `tests/onboarding-*.test.ts` — уникальность и полнота контента, route/role coverage, target contracts, versioned progress и безопасный reset.

**Playwright:** сквозной сценарий уровня A; responsive-матрица 1920×1080 → 768×1024; обязательные onboarding-сценарии для INITIATOR, DATA_OWNER, BOARD_MEMBER и ADMIN; missing-target recovery; cross-route ROLE/THESIS handoff; 18 golden screenshots.

---

## О демо-данных

- Показатели **внутри паспортов решений** (трудоёмкость, вероятности, суммы потерь) — **синтетические**; в интерфейсе они помечены бейджем «демо-данные».
- **Публичные показатели компании** на дашборде — фактические результаты АО «НАК «Казатомпром» за 1 полугодие 2026 года, официальный прогноз на 2026 год и показатели цифровизации из интегрированного годового отчёта за 2025 год. Факты и прогноз визуально разделены, рядом приведены ссылки на первоисточники ([lib/company.ts](lib/company.ts), [REAL-DATA-SOURCES.md](REAL-DATA-SOURCES.md)).
- Система нигде не показывает «фактический» финансовый эффект, не выведенный из введённых пользователем параметров.

Проектные решения и их обоснования зафиксированы в [DECISIONS.md](DECISIONS.md), декомпозиция работ — в [PLAN.md](PLAN.md).
